# Agent registry

Filled in live once all 4 windows are running. Session names are assigned by
the harness when each `claude` process starts (visible via `ListAgents`), so
this table can't be pre-filled — the PM populates it during bootstrap.

| Role     | Folder     | Session name | Status |
|----------|------------|--------------|--------|
| PM       | `/`        |              |        |
| Frontend | `frontend/`|              |        |
| Backend  | `backend/` |              |        |
| DB       | `db/`      |              |        |
