# DB Schema

Owned by the **db** agent. Update this whenever a Redis key pattern or MySQL
table changes — the backend agent treats this file as the truth.

## Redis key patterns
_(none yet — format: `key:pattern` → value shape, TTL, why)_

## MySQL schema (only if introduced — see docs/ARCHITECTURE.md)
_(none yet)_
